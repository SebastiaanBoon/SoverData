name: Deploy to Azure - Gemini 3.5 Flash

on:
  push:
    branches:
      - SoverData-Gemini-3.5-Flash

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Web App (Gemini 3.5 Flash)
        uses: azure/webapps-deploy@v3
        with:
          app-name: soverdata-gemini-35-flash
          package: .
